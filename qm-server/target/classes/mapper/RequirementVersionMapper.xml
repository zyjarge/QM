<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qm.requirement.mapper.RequirementVersionMapper">

    <insert id="insertVersion" parameterType="com.qm.requirement.entity.RequirementVersion">
        INSERT INTO requirement_versions (
            id, requirement_id, version_no, content, content_text,
            content_hash, fields_data, edited_by, change_summary,
            created_at, updated_at
        ) VALUES (
            #{id},
            #{requirementId},
            #{versionNo},
            #{content},
            #{contentText},
            #{contentHash},
            #{fieldsData},
            #{editedBy},
            #{changeSummary},
            NOW(), NOW()
        )
    </insert>

</mapper>
